import './landing.scss';
import { useNavigate } from 'react-router-dom';

const Landing = () => {
    return (
        <div className='landing-wrapper'>
            <h1>You are now booking!</h1>
        </div>
    );
};

export default Landing;